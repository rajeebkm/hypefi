const BlockedSocialIcon = ({ icon }: { icon: React.ReactNode }) => {
    return (
        <div className="p-2 bg-primary-500 rounded-lg">
            {icon}
        </div>
    )
}

export default BlockedSocialIcon;